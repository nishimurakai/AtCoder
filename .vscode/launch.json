{
    "configurations": [
        {
            "name": "debug py",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "args": [
                "<", "${fileDirname}/input.txt",
            ]
        }
    ]
}